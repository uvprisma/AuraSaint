<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Aurasaint - Redefiniendo el Streetwear con calidad y moda.">
    <title>Aurasaint | Faith Over Fear</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;700&family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        :root {
            --bg-dark: #050505;
            --bg-card: #0f0f0f;
            --gold: #D4AF37;
            --gold-gradient: linear-gradient(135deg, #bf953f, #fcf6ba, #b38728, #fbf5b7, #aa771c);
            --silver: #C0C0C0;
            --silver-gradient: linear-gradient(135deg, #e0e0e0, #ffffff, #a0a0a0);
            --text-main: #ffffff;
            --text-muted: #a0a0a0;
            --transition: all 0.4s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-main);
            overflow-x: hidden;
            line-height: 1.6;
        }

        h1, h2, h3 {
            font-family: 'Playfair Display', serif;
            letter-spacing: 1px;
        }

        a { text-decoration: none; color: inherit; transition: var(--transition); }
        ul { list-style: none; }

        /* --- HEADER & NAV --- */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 20px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(5, 5, 5, 0.9);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 1px solid rgba(212, 175, 55, 0.1);
        }

        .logo img {
            height: 50px; /* Ajusta según el logo */
            filter: drop-shadow(0 0 5px rgba(255,255,255,0.2));
        }

        nav ul {
            display: flex;
            gap: 30px;
        }

        nav a {
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
        }

        nav a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 1px;
            bottom: -5px;
            left: 0;
            background: var(--gold);
            transition: var(--transition);
        }

        nav a:hover { color: var(--gold); }
        nav a:hover::after { width: 100%; }

        /* --- HERO SECTION --- */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: radial-gradient(circle at center, #1a1a1a 0%, #000000 100%);
            position: relative;
            padding: 0 20px;
        }

        /* Efecto de luz superior */
        .hero::before {
            content: '';
            position: absolute;
            top: -100px;
            left: 50%;
            transform: translateX(-50%);
            width: 600px;
            height: 300px;
            background: radial-gradient(ellipse at center, rgba(255,255,255,0.15) 0%, rgba(0,0,0,0) 70%);
            pointer-events: none;
        }

        .hero-content {
            z-index: 2;
            opacity: 0;
            animation: fadeUp 1s forwards 0.5s;
        }

        .hero h1 {
            font-size: 4rem;
            margin-bottom: 20px;
            background: var(--silver-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-transform: uppercase;
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-muted);
            margin-bottom: 40px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .btn-gold {
            display: inline-block;
            padding: 15px 40px;
            background: transparent;
            border: 1px solid var(--gold);
            color: var(--gold);
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 700;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: var(--transition);
        }

        .btn-gold:hover {
            background: var(--gold);
            color: #000;
            box-shadow: 0 0 20px rgba(212, 175, 55, 0.4);
        }

        /* --- PRODUCTS / SERVICES --- */
        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 60px;
            color: var(--gold);
            position: relative;
        }
        
        .section-padding { padding: 100px 5%; }

        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            justify-items: center;
        }

        /* TARJETA DEL PRODUCTO MAGICA */
        .product-card {
            background: var(--bg-card);
            width: 100%;
            max-width: 400px;
            border: 1px solid #222;
            padding: 20px;
            text-align: center;
            transition: var(--transition);
            position: relative;
        }

        .product-card:hover {
            border-color: var(--gold);
            transform: translateY(-10px);
        }

        .image-container {
            position: relative;
            width: 100%;
            height: 450px; /* Altura fija para la imagen */
            overflow: hidden;
            margin-bottom: 20px;
        }

        .image-container img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover; /* Ajuste perfecto */
            transition: opacity 0.6s ease-in-out;
        }

        /* La magia: Ocultar la de atrás por defecto */
        .img-back { opacity: 0; }
        
        /* Al hacer hover, mostrar atrás y ocultar frente */
        .product-card:hover .img-back { opacity: 1; }
        .product-card:hover .img-front { opacity: 0; }

        .product-info h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #fff;
        }
        
        .product-info p {
            color: var(--silver);
            font-size: 0.9rem;
            margin-bottom: 15px;
        }

        .price {
            display: block;
            font-size: 1.2rem;
            color: var(--gold);
            margin-top: 10px;
            font-weight: bold;
        }

        /* --- ABOUT & INFO --- */
        .about-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            align-items: center;
        }

        .about-text h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #fff;
        }
        
        .highlight { color: var(--gold); }

        .info-box {
            background: #111;
            padding: 30px;
            border-left: 3px solid var(--gold);
            margin-top: 30px;
        }

        /* --- FAQ & FOOTER --- */
        .faq-item {
            background: #111;
            margin-bottom: 15px;
            padding: 20px;
            cursor: pointer;
            border: 1px solid #222;
        }
        
        .faq-item h4 { color: var(--silver); display: flex; justify-content: space-between; }
        .faq-content { display: none; margin-top: 15px; color: #888; }
        .faq-item.active .faq-content { display: block; }

        footer {
            background: #000;
            padding: 50px 5% 20px;
            border-top: 1px solid #222;
            text-align: center;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
            text-align: left;
        }

        .footer-col h4 { color: var(--gold); margin-bottom: 20px; font-size: 1.2rem; }
        .footer-col p, .footer-col a { color: #888; display: block; margin-bottom: 10px; font-size: 0.9rem; }
        .footer-col a:hover { color: #fff; }

        /* WhatsApp Button */
        .whatsapp-float {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: #25D366;
            color: #FFF;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.5);
            z-index: 9999;
            transition: var(--transition);
        }
        .whatsapp-float:hover { transform: scale(1.1); }

        /* ANIMATIONS */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* RESPONSIVE */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            .about-grid { grid-template-columns: 1fr; }
            nav { display: none; } /* En una versión completa haríamos menú hamburguesa */
            header { justify-content: center; }
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">
            <img src="logo.jpg" alt="Aurasaint Logo">
        </div>
        <nav>
            <ul>
                <li><a href="#inicio">Inicio</a></li>
                <li><a href="#productos">Colección</a></li>
                <li><a href="#nosotros">Nosotros</a></li>
                <li><a href="#contacto">Contacto</a></li>
            </ul>
        </nav>
        <a href="#productos" class="btn-gold" style="padding: 10px 20px; font-size: 0.8rem;">Comprar</a>
    </header>

    <section id="inicio" class="hero">
        <div class="hero-content">
            <h1>AuraSaint</h1>
            <p>Donde la calidad se encuentra con la moda. Redefine tu esencia.</p>
            <a href="#productos" class="btn-gold">Ver Colección</a>
        </div>
    </section>

    <section id="productos" class="section-padding">
        <h2 class="section-title">Nuestra Colección</h2>
        
        <div class="product-grid">
            <div class="product-card">
                <div class="image-container">
                    <img src="front.jpg" alt="Aurasaint Tee Front" class="img-front">
                    <img src="back.jpg" alt="Aurasaint Tee Back" class="img-back">
                </div>
                <div class="product-info">
                    <h3>Oversize "Faith Over Fear"</h3>
                    <p>Algodón premium. Corte perfecto. Diseño exclusivo.</p>
                    <span class="price">Disponible Ahora</span>
                    <br><br>
                    <a href="https://wa.me/526311295519?text=Hola,%20me%20interesa%20la%20camiseta%20Faith%20Over%20Fear" class="btn-gold" style="width: 100%; display:block; padding: 10px;">Ordenar</a>
                </div>
            </div>
            
            </div>
    </section>

    <section id="nosotros" class="section-padding" style="background: #0a0a0a;">
        <div class="about-grid">
            <div class="about-text">
                <h2>Sobre <span class="highlight">Aurasaint</span></h2>
                <p>Esta tienda se dedica a vender <strong>calidad y moda</strong>. No somos solo una marca de ropa, somos un movimiento.</p>
                
                <div class="info-box">
                    <h3 style="color: var(--silver); margin-bottom: 10px;"><i class="fas fa-tshirt"></i> Especialidad</h3>
                    <p style="color: #888;">Nuestra especialidad son las <strong>Camisetas Oversize</strong> con un fit diseñado para destacar.</p>
                </div>
            </div>
            <div class="about-faq">
                <h3 style="color: #fff; margin-bottom: 20px;">Preguntas Frecuentes</h3>
                
                <div class="faq-item" onclick="this.classList.toggle('active')">
                    <h4>¿Qué tallas están disponibles? <i class="fas fa-chevron-down"></i></h4>
                    <div class="faq-content">
                        Manejamos un corte Oversize premium. Contáctanos por WhatsApp para consultar el stock actual de S, M, L y XL.
                    </div>
                </div>

                <div class="faq-item" onclick="this.classList.toggle('active')">
                    <h4>¿Horarios de atención? <i class="fas fa-chevron-down"></i></h4>
                    <div class="faq-content">
                        Estamos disponibles de <strong>Lunes a Viernes de 9am a 9pm</strong>.
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer id="contacto">
        <div class="footer-grid">
            <div class="footer-col">
                <img src="logo.jpg" alt="Aurasaint" style="height: 40px; margin-bottom: 20px;">
                <p>Moda y Calidad superior.</p>
            </div>
            <div class="footer-col">
                <h4>Contacto</h4>
                <p><i class="fas fa-envelope"></i> menelchvz23@gmail.com</p>
                <p><i class="fas fa-phone"></i> 631 129 5519</p>
                <p><i class="fas fa-map-marker-alt"></i> Nogales, Sonora, MX</p>
            </div>
            <div class="footer-col">
                <h4>Legal</h4>
                <a href="#">Términos y Condiciones</a>
                <a href="#">Política de Privacidad</a>
            </div>
        </div>
        <div style="border-top: 1px solid #222; padding-top: 20px;">
            <p style="color: #555; font-size: 0.8rem;">&copy; 2026 Aurasaint. Todos los derechos reservados.</p>
        </div>
    </footer>

    <a href="https://wa.me/526311295519" class="whatsapp-float" target="_blank">
        <i class="fab fa-whatsapp"></i>
    </a>

</body>
</html>